# Pinned, deploy-friendly requirements for Python 3.10
# IMPORTANT: Do NOT generate this file via `pip freeze`.
# Use it as the source of truth for deployments (PythonAnywhere).

# --- Django + API ---
Django==5.2.7
djangorestframework==3.16.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
django-allauth==65.12.0

# --- Env / HTTP ---
python-decouple==3.8
python-dotenv==1.0.1
requests==2.32.5

# --- DB ---
psycopg2==2.9.11

# --- Media / Images ---
pillow==11.3.0
cloudinary==1.44.1

# --- Audio / ML (used by music MFCC + recommendations) ---
# NOTE: These versions are compatible with Python 3.10 on Linux.
numpy==2.2.6
scipy==1.15.3
scikit-learn==1.6.1
librosa==0.11.0

# --- Transitive runtime basics (keep minimal) ---
asgiref==3.10.0
certifi==2025.10.5
charset-normalizer==3.4.3
cryptography==46.0.2
idna==3.10
PyJWT==2.10.1
sqlparse==0.5.3
tzdata==2025.2
urllib3==2.5.0

gunicorn
